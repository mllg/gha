on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello

    steps:
    - name: checkout 
      uses: actions/checkout@v2
    - name: setup_r
      uses: mllg/setup_r_lib@master

